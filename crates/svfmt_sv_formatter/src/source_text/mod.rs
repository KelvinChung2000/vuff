//! Annex A.1 — `source_text` productions. The file-level root lives here
//! alongside, in the future, rules for `Description` alternatives.

pub(crate) mod root;

pub(crate) use root::SourceTextRoot;
